var OrderUtils, Utils, actionsList, actionsListReturnInfoState, helper, jsondiffpatch, _, _ref,
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

_ = require("underscore")._;

jsondiffpatch = require("jsondiffpatch");

Utils = require("./utils");

helper = require("../helper");

/*
Order Utils class
*/


OrderUtils = (function(_super) {
  __extends(OrderUtils, _super);

  function OrderUtils() {
    _ref = OrderUtils.__super__.constructor.apply(this, arguments);
    return _ref;
  }

  OrderUtils.prototype.actionsMapStatuses = function(diff, old_obj) {
    var actions;
    actions = [];
    _.each(actionsList(), function(item) {
      var action, key, obj, updated;
      key = item.key;
      obj = diff[key];
      if (obj) {
        updated = helper.getDeltaValue(obj);
        action = {
          action: item.action
        };
        action[key] = updated;
      }
      if (action) {
        return actions.push(action);
      }
    });
    return actions;
  };

  OrderUtils.prototype.actionMapReturnInfo = function(diff, old_obj, new_obj) {
    var action, actions, returnInfo, returnInfoDiff;
    actions = [];
    returnInfoDiff = diff['returnInfo'];
    if (returnInfoDiff) {
      if (_.isArray(returnInfoDiff)) {
        returnInfo = _.first(returnInfoDiff);
        action = {
          action: 'addReturnInfo'
        };
        _.each(_.keys(returnInfo), function(key) {
          return action[key] = returnInfo[key];
        });
        actions.push(action);
      } else {
        if (returnInfoDiff.items) {
          _.each(_.keys(returnInfoDiff.items), function(index) {
            var returnItem;
            returnItem = returnInfoDiff.items[index];
            return _.each(actionsListReturnInfoState(), function(actionDefinition) {
              var returnItemState;
              returnItemState = returnItem[actionDefinition.key];
              if (returnItemState) {
                action = {};
                action["action"] = actionDefinition.action;
                action["returnItemId"] = old_obj.returnInfo.items[index].id;
                action[actionDefinition.key] = helper.getDeltaValue(returnItemState);
                return actions.push(action);
              }
            });
          });
        }
      }
    }
    return actions;
  };

  return OrderUtils;

})(Utils);

/*
Exports object
*/


module.exports = OrderUtils;

actionsList = function() {
  return [
    {
      action: "changeOrderState",
      key: "orderState"
    }, {
      action: "changePaymentState",
      key: "paymentState"
    }, {
      action: "changeShipmentState",
      key: "shipmentState"
    }
  ];
};

actionsListReturnInfoState = function() {
  return [
    {
      action: "setReturnShipmentState",
      key: "shipmentState"
    }, {
      action: "setReturnPaymentState",
      key: "paymentState"
    }
  ];
};
